# 워크북 — 노션 학습 + AI 질문 브리지

작성일: 2026-09-08
작성자: 제옹(euijjang97)

## 1. 무엇을 만드나

챌린저는 노션으로 받은 워크북을 보며 학습합니다. 그 과정이 지금은 앱 바깥에 있습니다.
브라우저에 노션을 띄워 두고, 막히면 챗봇을 켜고, 그래도 안 풀리면 카카오톡에 물어봅니다.
세 곳에 흩어져 있고 어느 것도 UMC 안에 남지 않습니다.

맥 앱 안에 워크북을 들여옵니다. 왼쪽에 노션 웹뷰, 오른쪽에 AI 패널입니다.
본문을 드래그하면 인용이 패널에 물리고 AI와 주고받으며 질문을 다듬은 뒤,
그 질문을 커뮤니티 스레드로 보냅니다.

**이 기능의 목적은 학습 질문에 AI가 답하는 것이 아닙니다.** 막힌 지점이 사람에게
도달하게 만드는 것입니다. AI는 그 사이에서 질문을 다듬는 역할만 맡습니다.

## 2. 왜 이 모양인가

설계가 이렇게 생긴 이유는 제약 셋에서 나왔습니다. 셋 다 협상 대상이 아닙니다.

**첫째, 웹뷰 안 노션은 브라우저 안 노션보다 나쁩니다.** 확장이 없고 탭이 없고
로그인이 불편합니다. **둘째, 온디바이스 모델은 범용 챗봇보다 나쁩니다.**
"URLSession이 뭐야"에 그럴듯하게 틀린 답을 줍니다. 두 축 모두 지고 들어갑니다.
그대로 두면 챌린저는 그냥 브라우저를 켭니다.

그래서 이기려는 축을 옮겼습니다. **질문이 스레드로 흘러가는 경로**입니다.
브라우저에 띄운 범용 챗봇은 UMC 스레드에 글을 못 씁니다. 여기서만 되는 일이고
동시에 UMC의 실제 문제를 건드립니다 — 챌린저가 막혀도 질문을 안 합니다.
질문을 쓰는 게 부담스럽기 때문입니다. AI가 그 부담을 대신 집니다.

**셋째, FoundationModels 세션은 4,096 토큰이 전부입니다.**
instructions·프롬프트·출력이 모두 같은 예산에서 나갑니다
(`docs/claude/apple-frameworks/FoundationModels-Using-on-device-LLM-in-your-app.md:310`).
한글은 토큰 효율이 나빠 실질 4~6천 자 수준입니다. 워크북 한 페이지를 통째로 물리는
설계는 처음부터 불가능합니다.

그런데 목적을 "답하기"가 아니라 "질문 다듬기"로 잡으면 이 제약이 사라집니다.
필요한 입력은 **선택한 문단 하나와 사용자가 대충 쓴 질문**이 전부입니다.
창 안에 넉넉히 들어갑니다. 제약을 우회한 게 아니라 제약과 목적이 같은 방향을
보게 만들었습니다.

## 3. 화면 구조

기존 설계의 `NavigationSplitView` 3컬럼을 그대로 두되, 워크북을 고르면 **목록 컬럼만
접습니다.** `columnVisibility` 를 `.doubleColumn` 으로 바꾸면 됩니다.

```
┌────────┬──────────────────────────┬──────────────┐
│ 공지    │                          │              │
│ 스레드  │   Notion WebView         │   AI 패널     │
│ 워크북 ◀│                          │  (inspector)  │
│ 매칭    │                          │              │
└────────┴──────────────────────────┴──────────────┘
   유지        목록 컬럼이 접힌 자리        ⌘⌥I 토글
```

워크북은 목록 컬럼에 넣을 것이 없습니다. 노션 안에 이미 사이드바가 있어서
목록을 또 두면 사이드바가 둘이 됩니다. 그 칸을 통째로 본문 폭으로 씁니다.

**사이드바는 접지 않습니다.** 전체를 2단으로 바꾸면 항목을 오갈 때마다 레이아웃이
크게 흔들려 사용자가 위치를 잃습니다. 기존 설계가 사이드바를 역할에 따라 늘렸다 줄였다
하지 않기로 한 것과 같은 이유입니다.

AI 패널은 `.inspector(isPresented:)` 입니다. 직접 만든 분할 뷰가 아니라 시스템 인스펙터라
너비 조절과 접기가 공짜로 따라옵니다. 웹뷰는 `WebView(page)` 에 `WebPage` 를 물립니다
(`docs/claude/apple-frameworks/SwiftUI-WebKit-Integration.md:52`).

### 워크북에 들어가는 길

**앱이 워크북 링크를 알 필요가 없습니다.** 웹뷰를 노션 워크스페이스에 띄우고
그 다음은 챌린저가 노션 사이드바로 직접 찾아갑니다. 툴바에 주소 칸을 하나 두어
받은 링크를 붙여넣는 길도 같이 엽니다.

이렇게 두면 운영에서 기수·파트별 워크북 링크를 받아 오는 경로가 통째로 사라집니다.
링크를 서버에 넣고, 기수가 바뀔 때마다 갱신하고, 누락되면 화면이 비는 일이 전부 없어집니다.
대가는 첫 진입 때 챌린저가 스스로 한 번 찾아가야 한다는 것입니다.
마지막으로 본 주소를 저장해 다음 실행 때 그 자리에서 열면 그 한 번은 첫 회로 끝납니다.

붙여넣기를 허용하므로 도메인 화이트리스트가 그만큼 더 중요해집니다. 노션 밖 주소는
7장대로 기본 브라우저로 넘깁니다.

## 4. 모듈과 경계

```
Features/Workbook/
  Presentation/   WorkbookView · AIPanel · WorkbookViewModel(@Observable)
  Domain/         RefineQuestion · SendQuestionToThread
  Data/           없음
```

**Data 레이어가 없는 것이 이 설계의 핵심입니다.** 서버 호출은 전부 Community 모듈의
기존 UseCase를 부릅니다. 새 Router도, 새 DTO도, 서버 작업도 없습니다.
워크북 질문은 결국 스레드 메시지 한 건이기 때문입니다.

조각은 셋이고 서로를 모릅니다.

| 조각 | 하는 일 | 밖에 노출하는 것 | 모르는 것 |
|---|---|---|---|
| `WorkbookWebPage` | 노션 로드, 주소 입력·복원, 도메인 화이트리스트, 선택 텍스트 추출 | `selection: Quote?` | AI · 스레드 |
| `QuestionRefiner` | 세션 수명, 컨텍스트 압축, 스트리밍 | `messages` · `draft` | WebKit · 스레드 |
| `ThreadQuestionSender` | 참여 중인 방 목록, 인용 블록 포맷, 전송 | `send(quote:text:to:)` | WebKit · AI |

`WorkbookViewModel` 이 셋을 잇습니다. 셋 다 서로를 참조하지 않으므로 따로 테스트됩니다.
특히 `QuestionRefiner` 는 FoundationModels를 프로토콜 뒤에 두어 모델 없이도 테스트합니다.

## 5. 데이터 흐름

```
본문 드래그
  └→ callJavaScript("window.getSelection().toString()")
      └→ 인용 카드가 패널 상단에 고정      ← 매 턴 재전송하지 않는다
          └→ 채팅 (LanguageModelSession, 스트리밍)
              └→ 말풍선마다 [스레드에 물어보기]
                  └→ 방 선택 → 기존 메시지 전송 API
```

인용문은 세션 instructions에 한 번만 넣고 대화에는 넣지 않습니다.
매 턴 인용을 다시 실으면 네 턴 만에 창이 넘칩니다. 대화에는 순수한 대화만 쌓습니다.

말풍선마다 전송 버튼이 붙는 것이 이 화면의 유일한 안전장치입니다.
AI가 막히는 순간이 곧 탈출구가 되게 만듭니다.

### 인용 블록 포맷

```
> 📘 워크북 · {페이지 제목}
> {인용문}
> {URL}

{질문 본문}
```

스레드는 채팅방이라 질문이 묻히는 것을 막을 수 없습니다. 포맷을 고정하면 최소한
검색에는 걸립니다. 나중에 문의 도메인이 서면 이 포맷을 그대로 옮깁니다.

## 6. AI 계약

**이 기능에서 제일 중요한 자산은 instructions 문자열입니다.** 코드가 아니라 이 문자열이
품질을 정합니다. 인라인하지 않고 버전 붙은 리소스로 뺍니다.

계약은 셋입니다.

- 역할은 튜터가 아니라 **질문 정리 조수**입니다
- 기술적 사실을 단정하지 않습니다. 모르면 "이건 스레드에 물어보자"로 넘깁니다
- 출력은 짧게 유지합니다. 모델의 출력도 같은 4,096 토큰을 씁니다

두 번째 항목이 채팅 UI를 택한 대가를 치르는 부분입니다. 입력창을 열어 두면 챌린저는
반드시 학습 질문을 던집니다. 모델이 그때 아는 척을 하면 이 기능은 실패합니다.
막히는 것 자체는 실패가 아닙니다. 막혔을 때 스레드로 넘기면 설계대로 동작한 것입니다.

## 7. 실패 모드

### Apple Intelligence가 없는 기기

`SystemLanguageModel.default.availability` 는 Apple Silicon, Apple Intelligence 켜짐,
모델 다운로드 완료를 모두 요구합니다. 챌린저는 학생이라 Intel 맥이 섞여 있습니다.

**AI를 부가 기능으로 둡니다.** 모델이 없으면 패널이 채팅에서 질문 작성 폼으로
강등됩니다 — 인용 카드, 입력창, 방 선택, 전송. 브리지 자체는 AI 없이 온전히 돕니다.

핵심 가치가 AI에 걸려 있지 않다는 것이 이 설계에서 가장 중요한 속성입니다.
사용 불가 사유별로 안내만 갈라 줍니다(설정에서 켜기 · 다운로드 대기 · 기기 미지원).

### 컨텍스트 초과

멀티턴 채팅을 택했으므로 이것은 "혹시"가 아니라 "언제"입니다.

`exceededContextWindowSize` 를 잡아 이전 대화를 모델 자신에게 세 줄로 요약시키고
그 요약과 인용문으로 새 세션을 만든 뒤 실패한 프롬프트를 **한 번만** 재시도합니다.
사용자에게는 "이전 대화를 요약했어요" 한 줄만 보입니다. 두 번째도 실패하면
새 질문으로 시작하기 버튼을 내놓고 끝냅니다.

토큰을 미리 세지 않습니다. 에러를 잡아 대응하는 쪽이 코드가 짧고 정확합니다.
예방은 instructions의 출력 길이 제한으로 충분합니다.

### 노션 로그인

챌린저는 본인 노션 계정으로 워크스페이스에 들어갑니다. 웹뷰가 사실상 노션 세션을
들고 있는 미니 브라우저가 됩니다.

영구 데이터스토어를 쓰면 앱을 껐다 켜도 세션이 남습니다. 문제는 구글 로그인이
임베디드 웹뷰에서 막힐 가능성입니다. 막히면 이메일 코드 로그인으로 안내합니다.
**추정이며 9장의 스파이크에서 검증합니다.**

`NavigationDecider` 로 노션 도메인과 로그인에 필요한 도메인만 허용하고 나머지는
기본 브라우저로 넘깁니다. 웹뷰 안에서 아무 데나 갈 수 있게 두면 그 순간부터
미니 브라우저를 유지보수하게 됩니다.

### 선택 텍스트 추출

노션 에디터는 DOM이 단순하지 않아 `window.getSelection()` 이 빈 값을 줄 수 있습니다.
못 잡으면 인용 없이 질문만 보내는 경로로 떨어뜨립니다.
**이것도 9장의 스파이크에서 검증합니다.**

### 잠자기

워크북은 STOMP를 쓰지 않으므로 기존 설계의 재연결 처리와 무관합니다.
웹뷰는 시스템이 알아서 복구합니다. 추가 작업이 없습니다.

## 8. 1차 범위

| 뺀 것 | 이슈 | 착수 조건 |
|---|---|---|
| 스레드 답변을 워크북 화면에서 확인 | #13 | 브리지가 쓰이고 "답변 어디서 보냐"가 나올 때 |
| 학습 기록 — 하이라이트 · 메모 · 진도 | #14 | 브리지 사용이 확인된 뒤. 노션 앵커 안정성 검증이 선행 |
| Tool calling — 공지 · 마감일 조회 | #15 | 실제 대화의 토큰 사용량을 측정한 뒤 |

**워크북 다중 탭과 즐겨찾기는 이슈로 만들지 않았습니다.** 나중에 할 일이 아니라
하지 않기로 한 일입니다. 노션 사이드바와 3장의 마지막 주소 복원이 이미 그 일을 합니다.

1차에서 답변은 스레드 탭에서 확인합니다. 질문을 보낸 자리에서 답을 받는 것이
분명히 더 낫지만 그러려면 보낸 질문을 로컬에 쌓고 스레드 메시지를 역참조해야 합니다.
브리지가 실제로 쓰인다는 증거도 없이 치를 비용은 아닙니다.

## 9. 선행 스파이크

구현 계획을 쓰기 전에 30분짜리 확인을 먼저 돌립니다. 던져 버릴 코드입니다.

1. 빈 맥 앱에 `WebView` 하나 띄우고 노션 워크스페이스에 로그인해 봅니다.
   구글 로그인이 막히는지, 이메일 코드로 우회되는지, 앱을 껐다 켜도 세션이 남는지
2. 로그인된 상태에서 본문을 드래그하고 `callJavaScript("window.getSelection().toString()")`
   가 실제 텍스트를 주는지
3. 겸사겸사 온디바이스 모델에 한국어 질문 다듬기를 시켜 품질을 눈으로 봅니다

1번과 2번이 모두 실패하면 웹뷰 접근 자체를 재검토해야 합니다.
그 상태에서 구현 계획이 이미 나와 있으면 통째로 버리게 됩니다.

## 10. 테스트

`QuestionRefiner` 의 컨텍스트 초과 → 압축 → 재시도 경로에 테스트를 붙입니다.
이 기능에서 유일하게 복잡한 분기이고 실패하면 대화가 죽습니다.
모델 응답은 프로토콜로 갈아 끼웁니다.

인용 블록 포맷은 스냅샷 하나면 됩니다. `availability` 4분기의 UI 전환도 테스트합니다.
웹뷰 선택 추출은 자동화 비용이 값어치를 넘어서 수동 검증으로 둡니다.

## 11. 로드맵

기존 설계의 12장 표에 **2.5단계**로 끼웁니다.

| 단계 | 내용 | 끝났다고 볼 기준 |
|---|---|---|
| 2 | 스레드 목록 · 방 · 별도 창 | 스레드 방을 떼어 놓고 대화한다 |
| **2.5** | **워크북 웹뷰 + AI 질문 브리지** | **워크북을 읽다 막힌 지점을 스레드 질문으로 보낸다** |
| 3 | 운영진 모드, 공지 작성 · 읽음 통계 | 맥에서 쓴 공지가 iOS 앱에 뜬다 |

스레드 메시지 전송 API가 있어야 보낼 데가 생기므로 2단계가 하드 의존입니다.
반대로 3단계와는 아무 관계가 없습니다.

기존 설계 4장은 FoundationModels 기반 기능을 1차에서 뺐습니다. 거기서 가리킨 것은
공지 AI 초안과 스레드 자동 분류 둘이고, 그 둘은 지금도 뺀 채로 둡니다. 이 문서의 AI 패널은
없어도 브리지가 도는 부가 기능이라 같은 줄에 놓지 않았습니다.

기존 설계가 "챌린저 기능을 앞에 뒀습니다"라고 못을 박아 뒀고 워크북은 챌린저 전용
기능입니다. 그 원칙대로면 운영 기능인 3·4단계보다 앞이 맞습니다.
5단계 배포 전에는 반드시 들어가야 챌린저가 실제로 손에 쥡니다.

## 12. 확인이 필요한 것

- **웹뷰에서 노션 로그인이 통과하는가.** 9장 스파이크. 이 기능 전체가 여기 걸려 있습니다
- **노션 본문에서 선택 텍스트가 잡히는가.** 같은 스파이크
- **챌린저 기기 중 Apple Silicon 비율.** 낮으면 AI 없는 폴백이 주 경로가 됩니다.
  그 경우 채팅 UI에 들이는 공을 질문 작성 폼 쪽으로 옮겨야 합니다
- **온디바이스 모델의 한국어 질문 다듬기 품질.** 쓸 만하지 않으면 AI 패널을 빼고
  브리지만 남기는 선택지가 있습니다. 브리지 단독으로도 기능이 성립합니다
